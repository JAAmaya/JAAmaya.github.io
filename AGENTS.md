# Repository Notes

- Keep source files as UTF-8. Do not read/write project files with PowerShell `-Encoding Default` or other locale-dependent encodings; use UTF-8 explicitly to avoid mojibake in Spanish text.
